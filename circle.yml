machine:
  java:
    version: oraclejdk8

dependencies:
  override:
    - gradle build --stacktrace

test:
  override:
    - gradle test

deployment:
  production:
    branch: production
    commands:
      - gradle bintrayUpload