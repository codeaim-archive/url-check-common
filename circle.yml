machine:
  java:
    version: oraclejdk8

dependencies:
  override:
    - gradle build

test:
  override:
    - gradle test

deployment:
  production:
    branch: production
    commands:
      - gradle bintrayUpload