machine:
  java:
    version: oraclejdk8

dependencies:
  override:
    - gradle build

test:
  override:
    - gradle test

deployment:
  production:
    branch: production
    commands:
      - gradle -Puser=$BINTRAY_USER -Pkey=$BINTRAY_KEY bintrayUpload