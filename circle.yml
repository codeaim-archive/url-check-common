machine:
  java:
    version: oraclejdk8

dependencies:
  override:
    - chmod 777 ./gradlew
    - ./gradlew build

test:
  override:
    - gradle test

deployment:
  production:
    branch: production
    commands:
      - gradle bintrayUpload