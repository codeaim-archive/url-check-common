machine:
  java:
    version: oraclejdk8

dependencies:
  override:
    - gradle -version
    - gradle build --debug --stacktrace

test:
  override:
    - gradle test

deployment:
  production:
    branch: production
    commands:
      - gradle bintrayUpload