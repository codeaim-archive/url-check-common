machine:
  java:
    version: oraclejdk8

dependencies:
  override:
    - chmod 777 ./gradlew
    - ./gradlew build
    - gradle -version
    - gradle build --debug --stacktrace

test:
  override:
    - gradle test

deployment:
  production:
    branch: production
    commands:
      - gradle bintrayUpload